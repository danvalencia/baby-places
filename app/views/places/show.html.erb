<div class="col-lg-12">
  <h1 style="display: inline"><%= @place.name %></h1>
  <%= link_to 'Get Directions', map_url_for(@place) %>

  <br/>
  <img class="map-img" src="https://maps.googleapis.com/maps/api/staticmap?center=<%= @place.latitude %>,<%= @place.longitude %>&zoom=16&size=800x200&markers=color:green%7C<%= @place.latitude %>,<%= @place.longitude %>">

  <div>
    <strong>Amenities:</strong>
    <ul>
      <% @place.amenities.each do |amenity| %>
        <li>
          <%= amenity.name %>
        </li>
      <% end %>
    </ul>
  </div>

  <div>
    <strong>Address:</strong>
    <%= @place.address %>
  </div>

  <%= link_to 'Edit', edit_place_path(@place) %> |
  <%= link_to 'Back', root_path %>
</div>
